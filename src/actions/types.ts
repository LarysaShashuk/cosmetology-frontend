export interface newArticleType {
  title: string;
  text: string;
}

export interface articleIDType {
  id: string;
}

export interface updatedArticleType {
  id: string;
  title: string;
  text: string;
}
